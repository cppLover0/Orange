diff -Naur rust-libloading/src/os/unix/consts.rs rust-libloading-clean/src/os/unix/consts.rs
--- rust-libloading/src/os/unix/consts.rs	2006-07-24 05:21:28.000000000 +0400
+++ rust-libloading-clean/src/os/unix/consts.rs	2026-06-09 20:39:18.132080327 +0300
@@ -89,6 +89,7 @@
             target_os = "nto",
             target_os = "hurd",
             target_os = "cygwin",
+            target_os = "orange"
         ))] {
             pub(super) const RTLD_LAZY: c_int = 1;
         } else {
@@ -129,6 +130,7 @@
             target_os = "nto",
             target_os = "hurd",
             target_os = "cygwin",
+            target_os = "orange"
         ))] {
             pub(super) const RTLD_NOW: c_int = 2;
         } else if #[cfg(all(target_os = "android",target_pointer_width = "32"))] {
@@ -182,6 +184,7 @@
             target_os = "redox",
             target_os = "nto",
             target_os = "hurd",
+            target_os = "orange"
         ))] {
             pub(super) const RTLD_GLOBAL: c_int = 0x100;
         } else {
@@ -228,6 +231,7 @@
             target_os = "redox",
             target_os = "hurd",
             target_os = "cygwin",
+            target_os = "orange"
         ))] {
             pub(super) const RTLD_LOCAL: c_int = 0;
         } else {
