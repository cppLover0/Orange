diff -Naur rust-polling/src/epoll.rs rust-polling-clean/src/epoll.rs
--- rust-polling/src/epoll.rs	2006-07-24 05:21:28.000000000 +0400
+++ rust-polling-clean/src/epoll.rs	2026-06-13 19:45:14.738940364 +0300
@@ -4,9 +4,9 @@
 use std::os::unix::io::{AsFd, AsRawFd, BorrowedFd, RawFd};
 use std::time::{Duration, Instant};
 
-#[cfg(not(target_os = "redox"))]
+#[cfg(not(any(target_os = "redox", target_os = "orange")))]
 use rustix::event::{eventfd, EventfdFlags};
-#[cfg(not(target_os = "redox"))]
+#[cfg(not(any(target_os = "redox", target_os = "orange")))]
 use rustix::time::{
     timerfd_create, timerfd_settime, Itimerspec, TimerfdClockId, TimerfdFlags, TimerfdTimerFlags,
 };
@@ -32,7 +32,7 @@
     /// File descriptor for the timerfd that produces timeouts.
     ///
     /// Redox does not support timerfd.
-    #[cfg(not(target_os = "redox"))]
+    #[cfg(not(any(target_os = "redox", target_os = "orange")))]
     timer_fd: Option<OwnedFd>,
 }
 
@@ -46,7 +46,7 @@
 
         // Set up notifier and timerfd.
         let notifier = Notifier::new()?;
-        #[cfg(not(target_os = "redox"))]
+        #[cfg(not(any(target_os = "redox", target_os = "orange")))]
         let timer_fd = timerfd_create(
             TimerfdClockId::Monotonic,
             TimerfdFlags::CLOEXEC | TimerfdFlags::NONBLOCK,
@@ -56,12 +56,12 @@
         let poller = Poller {
             epoll_fd,
             notifier,
-            #[cfg(not(target_os = "redox"))]
+            #[cfg(not(any(target_os = "redox", target_os = "orange")))]
             timer_fd,
         };
 
         unsafe {
-            #[cfg(not(target_os = "redox"))]
+            #[cfg(not(any(target_os = "redox", target_os = "orange")))]
             if let Some(ref timer_fd) = poller.timer_fd {
                 poller.add(
                     timer_fd.as_raw_fd(),
@@ -176,7 +176,7 @@
 
         let timeout = deadline.map(|deadline| deadline.saturating_duration_since(Instant::now()));
 
-        #[cfg(not(target_os = "redox"))]
+        #[cfg(not(any(target_os = "redox", target_os = "orange")))]
         if let Some(ref timer_fd) = self.timer_fd {
             // Configure the timeout using timerfd.
             let new_val = Itimerspec {
@@ -203,9 +203,9 @@
             )?;
         }
 
-        #[cfg(not(target_os = "redox"))]
+        #[cfg(not(any(target_os = "redox", target_os = "orange")))]
         let timer_fd = &self.timer_fd;
-        #[cfg(target_os = "redox")]
+        #[cfg(any(target_os = "redox", target_os = "orange"))]
         let timer_fd: Option<core::convert::Infallible> = None;
 
         // Timeout for epoll. In case of overflow, use no timeout.
@@ -277,7 +277,7 @@
         #[cfg(feature = "tracing")]
         let _enter = span.enter();
 
-        #[cfg(not(target_os = "redox"))]
+        #[cfg(not(any(any(target_os = "redox", target_os = "orange"))))]
         if let Some(timer_fd) = self.timer_fd.take() {
             let _ = self.delete(timer_fd.as_fd());
         }
@@ -286,7 +286,7 @@
 }
 
 /// `timespec` value that equals zero.
-#[cfg(not(target_os = "redox"))]
+#[cfg(not(any(any(target_os = "redox", target_os = "orange"))))]
 const TS_ZERO: Timespec = unsafe { std::mem::transmute([0u8; std::mem::size_of::<Timespec>()]) };
 
 /// Get the EPOLL flags for the interest.
@@ -420,7 +420,7 @@
 #[derive(Debug)]
 enum Notifier {
     /// The primary notifier, using eventfd.
-    #[cfg(not(target_os = "redox"))]
+    #[cfg(not(any(target_os = "redox", target_os = "orange")))]
     EventFd(OwnedFd),
 
     /// The fallback notifier, using a pipe.
@@ -437,7 +437,7 @@
     /// Create a new notifier.
     fn new() -> io::Result<Self> {
         // Skip eventfd for testing if necessary.
-        #[cfg(not(target_os = "redox"))]
+        #[cfg(not(any(target_os = "redox", target_os = "orange")))]
         {
             if !cfg!(polling_test_epoll_pipe) {
                 // Try to create an eventfd.
@@ -476,7 +476,7 @@
     /// The file descriptor to register in the poller.
     fn as_fd(&self) -> BorrowedFd<'_> {
         match self {
-            #[cfg(not(target_os = "redox"))]
+            #[cfg(not(any(target_os = "redox", target_os = "orange")))]
             Notifier::EventFd(fd) => fd.as_fd(),
             Notifier::Pipe {
                 read_pipe: read, ..
@@ -487,7 +487,7 @@
     /// Notify the poller.
     fn notify(&self) {
         match self {
-            #[cfg(not(target_os = "redox"))]
+            #[cfg(not(any(target_os = "redox", target_os = "orange")))]
             Self::EventFd(fd) => {
                 let buf: [u8; 8] = 1u64.to_ne_bytes();
                 let _ = write(fd, &buf);
@@ -502,7 +502,7 @@
     /// Clear the notification.
     fn clear(&self) {
         match self {
-            #[cfg(not(target_os = "redox"))]
+            #[cfg(not(any(target_os = "redox", target_os = "orange")))]
             Self::EventFd(fd) => {
                 let mut buf = [0u8; 8];
                 let _ = read(fd, &mut buf);
diff -Naur rust-polling/src/lib.rs rust-polling-clean/src/lib.rs
--- rust-polling/src/lib.rs	2006-07-24 05:21:28.000000000 +0400
+++ rust-polling-clean/src/lib.rs	2026-06-13 19:29:16.660688571 +0300
@@ -83,7 +83,8 @@
     } else if #[cfg(any(
         target_os = "linux",
         target_os = "android",
-        target_os = "redox"
+        target_os = "redox",
+        target_os = "orange"
     ))] {
         mod epoll;
         use epoll as sys;
@@ -983,7 +984,8 @@
         target_os = "freebsd",
         target_os = "netbsd",
         target_os = "openbsd",
-        target_os = "dragonfly",
+        target_os = "orange",
+        target_os = "dragonfly"
     ),
     not(polling_test_poll_backend),
 ))]
@@ -999,7 +1001,8 @@
         target_os = "freebsd",
         target_os = "netbsd",
         target_os = "openbsd",
-        target_os = "dragonfly",
+        target_os = "orange",
+        target_os = "dragonfly"
     )))
 )]
 mod raw_fd_impl {
